# Core

This module is reserved for common strongly typed data structures and unit-safe domain types.

It must remain independent of Qt, RoboDK, ROS, vendor SDKs, and avoid OpenCV in public APIs where practical.
