# Adapter SDK

This module is reserved for neutral robot, camera, session reader, and session writer interfaces.

Vendor SDKs, proprietary code, and dynamic plugin loading should remain outside the core MVP.
