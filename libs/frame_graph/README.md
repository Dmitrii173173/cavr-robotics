# Frame Graph

This module is reserved for timestamped parent-child transforms, frame validation, cycle detection, and transform lookup.

Internal conventions should use meters, radians, normalized quaternions, and explicit SE(3) transforms.
