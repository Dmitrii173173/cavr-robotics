# Schemas

This directory is reserved for future Protocol Buffers schemas and MCAP schema registration metadata.

The MVP should not depend on MCAP or Protocol Buffers until deterministic CSV and image replay is working.
